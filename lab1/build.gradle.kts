plugins {
    id("com.diffplug.spotless") version "8.10.2"
}

subprojects {
    /*
    apply(plugin = "com.diffplug.spotless")

    spotless{
        java{
            googleJavaFormat().aosp()
        }
    }
    */
}
